{
  "name": "bridge",
  "packageManager": "yarn@4.10.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "biome check .",
    "format": "biome format --write .",
    "db:generate:types": "supabase gen types typescript --local > packages/shared/src/database/database.types.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0"
  }
}
